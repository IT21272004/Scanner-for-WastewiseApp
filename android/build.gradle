allprojects {
    repositories {
        google()
        mavenCentral()
    }

    // dependencies {
    //     classpath 'com.android.tools.build:gradle:4.2.0' // Update to the latest Gradle version
    //     classpath 'com.google.gms:google-services:4.3.10' // (if applicable)
    //     classpath 'com.google.firebase:firebase-crashlytics-gradle:2.8.1' // (if using Firebase Crashlytics)
    //     classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1' // (if required)
    //     classpath 'dev.flutter:flutter-gradle-plugin:3.19.4' // Update to the Flutter version you are using
    // }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
